#pragma once

#include <string>
#include <sstream>

struct BtAdapter {
    BtAdapter() {}
};

struct BleDevice {
    BleDevice() {}
};

struct GattClient {
    GattClient() {}
};

struct GattCharacteristic {
    GattCharacteristic() {}
};

